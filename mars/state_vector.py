import numpy as np


class State():

    def __init__(self, grid, problem):
        self.rho = np.meshgrid[2*self.gz + self.nx3,
                               2*self.gz + self.nx2,
                               2*self.gz + self.nx1]
